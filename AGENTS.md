# 族谱网站开发规范

## 1. 项目定位

一个纯前端、本地数据驱动的族谱可视化网站。采用“思维导图”式布局：从上到下展示家族代际关系，每个节点显示人物姓名，点击节点在屏幕中央弹出该人物的详细介绍弹窗。

## 2. 技术栈

- **HTML5**：语义化结构，单入口页面 `index.html`。
- **CSS3**：原生 CSS，使用 CSS 变量管理主题色，不依赖任何 UI 框架。
- **JavaScript**：原生 ES Modules，不依赖 React/Vue/jQuery 等第三方库。
- **后端**：Node.js + Express + Multer，用于持久化保存 `data/family.json` 和 `images/` 目录下的头像。
- **数据存储**：
  - 运行时通过后端 API 保存到服务器本地文件（`data/family.json`）。
  - 后端不可用时回退到浏览器 `localStorage`。
  - 可通过「导出 JSON」/「导入 JSON」进行备份与恢复。

## 3. 数据规范

### 3.1 文件位置

```
project/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── tree.js
│   ├── modal.js
│   └── data.js
└── data/
    └── family.json
```

### 3.2 JSON 结构

```json
{
  "familyName": "张氏",
  "rootId": "p1",
  "people": [
    {
      "id": "p1",
      "name": "张 ancestor",
      "gender": "male",
      "birth": "1900",
      "death": "1980",
      "spouse": "李氏",
      "children": ["p2", "p3"],
      "photo": "images/p1.jpg",
      "bio": "家族始祖，...",
      "notes": "其他备注"
    }
  ]
}
```

### 3.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | 唯一标识，建议 `p` + 数字 |
| `name` | string | 是 | 显示在节点上的姓名 |
| `gender` | string | 否 | `male` / `female`，用于区分节点颜色 |
| `birth` | string | 否 | 生年，可只写年份 |
| `death` | string | 否 | 卒年，健在则留空 |
| `spouse` | string / string[] | 否 | 配偶姓名 |
| `children` | string[] | 否 | 子女 id 列表，决定树形分支 |
| `photo` | string | 否 | 头像图片路径（如 `/images/G2-20250705143022.jpg`）或 Base64 Data URL；后端上传时按 `G{代数}-{年月日时分秒}` 命名 |
| `address` | string | 否 | 居住地址 |
| `bio` | string | 否 | 详细介绍，支持纯文本或简单 HTML |
| `notes` | string | 否 | 备注 |

### 3.5 世代计算

- 以 `rootId` 指定的始祖为**第 1 世**。
- 每向下一代，世数加 1。
- 首页节点、详情弹窗、后台树形列表均会显示该成员的世数。

## 4. 页面结构与渲染

### 4.1 布局方向

- 树形方向：**从上到下**（Top-to-Bottom）。
- 根节点位于页面顶部居中，子代依次向下展开。
- 节点之间使用 SVG 贝塞尔曲线连接，线条不经过节点文字区域。
- 首页顶部提供姓名搜索框，输入时实时列出匹配成员，点击后自动定位到该节点并打开详情弹窗。

### 4.2 节点设计

- 节点为可点击卡片，显示：姓名（主文字）、生卒年份（副文字，可选）。
- 节点最小尺寸不小于 80×44px，触控区域不小于 44×44px。
- 男性节点与女性节点使用不同边框色或背景色区分（由 CSS 变量控制）。
- 当前选中节点高亮显示。

### 4.3 缩放与平移

- 支持鼠标滚轮缩放、拖拽画布平移。
- 初始化时自动将根节点置于可视区域顶部居中。
- 移动端支持双指缩放和单指拖动。

## 5. 弹窗交互

### 5.1 触发方式

- 点击任意人物节点，在屏幕正中央弹出详情弹窗。
- 点击弹窗外部区域或关闭按钮，弹窗关闭。
- 按 `Esc` 键关闭弹窗。

### 5.2 弹窗内容

- 头像（如果有）
- 姓名
- 生卒年份
- 配偶
- 详细介绍 `bio`
- 备注 `notes`

### 5.3 弹窗样式

- 固定宽度，最大宽度不超过屏幕宽度的 90%。
- 遮罩层背景色半透明黑色。
- 内容过多时内部滚动，弹窗本身不超出屏幕。

## 6. 样式规范

### 6.1 主题变量

在 `css/style.css` 顶部定义：

```css
:root {
  --bg-color: #f8f5f0;
  --node-bg: #ffffff;
  --node-border: #8b5e3c;
  --male-color: #4a90d9;
  --female-color: #e07a8a;
  --line-color: #b0a090;
  --text-color: #333333;
  --modal-overlay: rgba(0, 0, 0, 0.5);
}
```

### 6.2 响应式要求

- 桌面端（≥1024px）：完整树形图，节点间距宽松。
- 平板端（768px–1023px）：适度缩小节点尺寸和间距。
- 手机端（<768px）：
  - 节点宽度自适应，文字可折行。
  - 默认缩放比例适当缩小，确保首屏可见根节点及一代子节点。
  - 弹窗宽度接近全屏。

### 6.3 字体与图标

- 中文字体优先使用系统默认无衬线字体栈：`"PingFang SC", "Microsoft YaHei", sans-serif`。
- 不使用第三方图标库，关闭按钮、缩放按钮等使用纯 CSS 绘制或 Unicode 字符。

## 7. JavaScript 规范

### 7.1 模块划分

| 文件 | 职责 |
|------|------|
| `js/data.js` | 加载 `data/family.json`，提供数据查询接口 |
| `js/tree.js` | 计算树形布局、渲染节点与连线、处理缩放平移 |
| `js/modal.js` | 弹窗的打开、关闭、内容填充 |
| `js/main.js` | 初始化入口，组合各模块 |

### 7.2 核心算法

- 树形布局使用递归计算每个节点的 `x`、`y` 坐标。
- 子节点在父节点下方水平均匀分布。
- 同一层节点避免重叠， sibling 之间保留最小水平间距。

### 7.3 事件处理

- 节点点击：`event.stopPropagation()` 后打开弹窗。
- 画布拖拽：监听 `mousedown` / `mousemove` / `mouseup`，移动端对应 `touch*` 事件。
- 缩放：监听 wheel 事件，以鼠标位置为中心缩放。

## 8. 性能与可访问性

- 家族人数在 500 人以内时，页面应流畅运行。
- 图片懒加载：头像仅在进入可视区域附近时加载。
- 支持键盘导航：Tab 键可在节点间切换，Enter 键打开弹窗。
- 为节点添加 `role="button"` 和 `aria-label`。

## 9. 数据维护流程

1. 启动后端服务：`npm install` 安装依赖后，运行 `npm start`。
2. 通过后台管理页增删改人物，数据会自动写入 `data/family.json`。
3. 后台顶部显示数据概览：人员总数、男性数量、女性数量、繁衍世代数、男女比例条。
4. 上传的头像会自动保存到 `images/` 目录，并在 `photo` 字段引用 `/images/xxx.jpg`。
5. 如需备份，复制 `data/family.json` 和 `images/` 目录。

## 10. 部署

- 运行 `npm install` 安装 `express` 和 `multer`。
- 运行 `npm start` 启动服务（默认端口 3000）。
- 生产环境建议使用 PM2 或 systemd 保持服务运行。
- 如需外网访问，用 Nginx 反向代理到 `http://127.0.0.1:3000`。
